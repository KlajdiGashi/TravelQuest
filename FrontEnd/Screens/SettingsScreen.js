import React from 'react';
import { View, Text } from 'react-native';

const SettingsScreen = () => {
    return (
        <View>
            <Text>Settings Screen</Text>
            {/* Ive only added the files to fix the App.js navigation*/}
        </View>
    );
};

export default SettingsScreen;